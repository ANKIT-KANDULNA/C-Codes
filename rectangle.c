#include <stdio.h>
float main()
{
    float l,b;
    printf("enter ths length:");
    scanf("%f",&l);
    printf("enter the breadth:");
    scanf("%f",&b);
    printf("Perimeter of the Rectangle is:%f",2*(l+b));
    return 0;

}