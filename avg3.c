#include <stdio.h>
float main()
{
    float a,b,c,d;
    printf("enter the first number:");
    scanf("%f",&a);
    printf("enter the second number:");
    scanf("%f",&b);
    printf("enter the third number:");
    scanf("%f",&c);
    printf("the average of these numbers are:%f",(a+b+c)/3);
    return 0;
}
